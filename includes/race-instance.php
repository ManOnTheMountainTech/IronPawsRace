<?php
    namespace IronPaws;

    defined( 'ABSPATH' ) || exit;
    // Returns a login form if the user is not logged on
    // @return: if not logged in, the logon form
    //          if logged in, null
    class Race_Instance {
        const START_DATE_TIME = 3;
        const RACE_DEFS_FK = 2;
    }
?>